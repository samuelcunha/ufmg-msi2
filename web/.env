# Public API URL, used by the browser. Vercel's own env var (set via
# `vercel env add`) is what actually applies in production builds — this
# file only matters for `npm run build`/`next start` run outside Vercel.
NEXT_PUBLIC_API_URL=https://your-api-domain.example.com/
